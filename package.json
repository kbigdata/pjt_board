{
  "name": "kanflow",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo run dev",
    "dev:frontend": "turbo run dev --filter=@kanflow/frontend",
    "dev:backend": "turbo run dev --filter=@kanflow/backend",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:watch": "turbo run test -- --watch",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint -- --fix",
    "db:migrate": "cd apps/backend && npx prisma migrate dev",
    "db:migrate:prod": "cd apps/backend && DATABASE_URL=\"postgresql://kanflow:kanflow_prod_2026@localhost:5432/kanflow_prod\" npx prisma migrate deploy",
    "db:seed": "cd apps/backend && npx prisma db seed",
    "db:studio": "cd apps/backend && npx prisma studio",
    "docker:dev": "docker compose -f docker/dev/docker-compose.yml up -d",
    "docker:prod": "docker compose -f docker/prod/docker-compose.yml up -d",
    "docker:down": "docker compose -f docker/dev/docker-compose.yml down && docker compose -f docker/prod/docker-compose.yml down"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
